// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mm", "for mm support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mm", "enable mm support", "no");

if (PHP_MM != "no") {
	EXTENSION("mm", "mm.c");
}

